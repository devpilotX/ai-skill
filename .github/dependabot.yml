version: 2
updates:
  # Keeps the SHA pinned actions in .github/workflows current. The scripts have
  # no third party Python dependencies, so there is no pip ecosystem to watch.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "bump"
    groups:
      actions:
        patterns: ["*"]
